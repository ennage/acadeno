@page "/"
@using Acadeno.Backend.Services

@inject AuthService AuthEngine
@inject NavigationManager NavManager

<!-- HTML -->

<main class="login-container">
    <div class="left-panel">
        <span class="copyright">© 2025 Acadeno</span>
    </div>

    <div class="right-panel">
        <h1>Acadeno</h1>

        @* ADDED: @onsubmit="HandleLogin" - This links the form to C# *@
        @* REMOVED: action="..." - Because we handle it in C# now *@
        <form @onsubmit="HandleLogin">
            <input type="email" placeholder="Enter your Email" @bind=loginEmail required>
            <input type="password" placeholder="Enter your password" @bind=loginPassword required>
            
            <div class="form-options">
                <label><input type="checkbox"> Remember Me</label>
                <a href="#">Forget Password</a>
            </div>

            <button type="submit">LOGIN</button>
        </form>

        <div class="social-login">
            <p>or sign in with:</p>
            <div class="social-icons">
                <span class="google">Google</span>
                <span class="facebook">facebook</span>
            </div>
        </div>

        <p class="signup-link">Don’t have an account? <a href="#">Sign up now.</a></p>
    </div>
</main>

<!-- CONNECTOR -->

@code {
    private string loginEmail = "";
    private string loginPassword = "";
    private string errorMsg = "";

    async Task HandleLogin()
    {
        // 1. Debug check: Did the UI actually capture the text?
        Console.WriteLine($"Login attempt for: {loginEmail}");

        try 
        {
            // 2. Call the Logic Dev's fixed engine
            var user = await AuthEngine.LoginUser(loginEmail, loginPassword);

            if (user != null)
            {
                Console.WriteLine("Success! Redirecting...");
                NavManager.NavigateTo("/dashboard");
            }
            else
            {
                errorMsg = "Invalid Email or Password.";
            }
        }
        catch (Exception ex)
        {
            errorMsg = "Critical Error: " + ex.Message;
            Console.WriteLine($"CRASH: {ex}");
        }
    }
}