@using Acadeno.Backend.Services
@inject NavigationManager NavManager
@inject UserService UserService

<aside class="sidebar">
    <div class="sidebar-header">
        <div class="logo"> ACADENYO </div>
        <div class="user-info">
                <h2>@(UserService.CurrentUser?.Name ?? "Guest")</h2>
                <p>@UserService.CurrentUser?.University</p>
        </div>
    </div>

    @* --- SIDEBAR --- *@
    <nav class="sidebar-nav">
        <div class="nav-button">   
            <NavLink class="nav-link" href="dashboard" Match="NavLinkMatch.All">
                Dashboard
            </NavLink>
        </div>
        <div class="nav-button">
            <NavLink class="nav-link" href="courses">
                Courses
            </NavLink>
        </div>
        <div class="nav-button">
            <NavLink class="nav-link" href="calendar">
                Calendar
            </NavLink>
        </div>
        <div class="nav-button">
            <NavLink class="nav-link" href="about">
                About
            </NavLink>
        </div>
    </nav>
    
    @* --- SIDEBAR FOOTER --- *@
    <div class="sidebar-footer">
        <!-- LOGOUT -->
        <button class="logout-nav logout-btn" @onclick="HandleLogout">
            Logout
        </button>
        <!-- COPYRIGHT -->
        <p class="copyright">© @DateTime.Now.Year ACADENO</p>
    </div>
</aside>

@code 
{
    private void HandleLogout()
    {
        UserService.Logout();
        NavManager.NavigateTo("/");
    }
}